#include <stdio.h>
#include "ps.h"

int	verify_input()
{
	if arg
}

int	main(int argc, char *argv[])
{
	if (argc <= 1)
		return (0);
	if (argc >= 2)
	{
		if(verify_input(argc, argv))
		{
			
		}
		else
			ft_putstr_fd("Error", 1);
	}

	return (0);
}